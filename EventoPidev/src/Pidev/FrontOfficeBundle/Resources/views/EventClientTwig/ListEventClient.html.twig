{# empty Twig template #}

{% extends "PidevFrontOfficeBundle::EvenementClient.html.twig" %} 


{% block eventListe %}
                   {% for message in app.session.flashbag.get('info') %}
    <div class="alert alert-info alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert">
            <span aria-hidden="true">&times;</span>
            <span class="sr-only">Close</span>
        </button>
        <p>{{ message }}</p>
    </div>
{% endfor %}
    
    {% for event in events %}
        <div class="tbl-item" >
            
<div class="col-lg-3" >
  <div class="member-team" style="width:350px;height:400px;">
      
     
      <img src="{{event.url}}" class="img-responsive" style="width:290px; height:40%;" id="test1"/>

                                                    <h3 style="width:100%;height:20%;overflow:hidden;">{{ event.titre }}
                                                        <small>{{ event.type }}</small>
                                                    </h3>
                                                        <p style="width:100%;height:15%;overflow: auto;">du <b>{{event.dateDebut|date('d/m/Y')}}</b> au <b>{{event.dateFin|date('d/m/Y')}}</b><br>a <b>{{ event.lieu }}</b></p>
                                                    
                                                      
                                                    <ul class="social-icons list-unstyled list-inline mbl mtl" align="center" >
                                                        
                                                        <li>{{ facebookButton({'url' : "http://localhost/PiDev/eventopidev/web/app_dev.php/detailsEvente/" ~ event.id ~ "?url=" ~event.url , 'layout' : 'icon'}) }}</li>
                                                        <li><a href="{{path('participer_evenement',{'id':event.id})}}" data-hover="tooltip" data-original-title="Participer" class="googleplus"><i class="fa fa-hand-o-right"></i></a></li>
                                                        <li><a href="{{path('details_evenement',{'id':event.id,'url':event.url})}}" data-hover="tooltip" data-original-title="Details" class="skype"><i class="fa fa-plus-circle"></i></a></li>
                                                        <li><a href="{{path('pidev_front_office_Invitation_client_list',{'ide':event.id})}}" data-hover="tooltip" data-original-title="Inviter" class="twitter"><i class="fa fa-map-marker"></i></a></li>
                                                    </ul>
       
                                                </div>
                                                    
                                            </div>
                                                    </div>
        {% endfor %}
        {% endblock %}